# 知影 APK 静态架构复盘

分析对象：用户提供的 `base.apk.1`（SHA-256：`CB409B680F030B30E17915B161ED1FB716E774E1CF1EF8E6179DF5D4A53D691D`）。本复盘仅使用压缩包目录、Android manifest 和内嵌文本资源的静态读取；没有安装、运行或连接该 App 的服务端。

## 可确认的架构

- Android 外壳：`com.zhiying.lab`，最低 API 24、目标 API 36；采用 Capacitor 运行 Web 前端。
- 原生能力：Capacitor App、Browser、Haptics、Local Notifications；另带 ML Kit OCR 原生模型。
- 前端分层：内嵌的 HTML/CSS/JavaScript 资源按系统与功能拆分；病例图、笔记、题库和课程资源随包交付，适合离线浏览。
- 本地数据：课程、知识卡、DICOM 与备份概念分开；DICOM 去标识逻辑、浏览器本地存储和按需云端同步均是独立模块。

## 值得借鉴的产品思路

1. **病例到知识卡的闭环**：把影像表现、关键征象、诊断标准、鉴别、检查策略、易错点和随访拆成可复习模块，而不是只保存一段自由笔记。
2. **本地优先、同步可选**：数据先留在设备；备份必须由用户明确触发，并先检查 DICOM 是否脱敏。
3. **复习与练习分开**：知识卡按记忆强度安排复习，诊断训练则保留独立答题记录，避免把“背过”伪装成诊断能力。
4. **设备适配明确**：手机和平板用不同信息密度，不把桌面布局简单缩小到手机。

## 不应直接照搬的部分

- APK 中的病例图片、课程文字、题库、界面设计与品牌资源均可能受著作权、许可或合同限制。除非取得权利人书面授权，影研社不得重新发布、复制或改写后作为其内容。
- 包中存在安装更新、通知、后台唤醒等权限；影研社未来的移动端只应在明确需求、最小权限和隐私说明齐备时接入。
- 去标识不能只靠清空 DICOM 元数据。烧录文字、像素中的身份信息以及跨文件关联仍需人工和专业工具复核。

## 影研社的实施决策

本次将“结构化病例学习卡”以原创实现接入个人影像库：固定模板、可编辑内容、间隔复习和纯本地保存。不会从 APK 导入任何图像、题库、病案文本或品牌素材。

未来移动端建议采用现有 Web 的离线优先架构，优先考虑 PWA；只有需要本地 DICOM 文件访问、可靠通知或硬件能力时，再评估 Capacitor 外壳。云端仅同步经用户确认的学习数据，并默认禁止上传未脱敏 DICOM。
